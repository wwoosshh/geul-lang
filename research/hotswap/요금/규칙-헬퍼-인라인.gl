(* 규칙-헬퍼.gl 의 잎 제약 해법: 헬퍼를 손으로 인라인한다. --조각 이 받는다.
   같은 의미인데 읽기는 더 나쁘다 — 규칙이 복잡해질수록 이 비용이 커진다. *)

[정수 주문금액을 정수 수량을 배송비]는 -> 정수 {
    주문금액 >= 50000이면 { 반환 0. }
    반환 ((3000 + 137 * 수량 + 99) / 100) * 100.
}

(* 배송비 규칙 — 순수 잎 함수. --조각 으로 컴파일해 실행 중 교체한다.
   주문금액(원), 수량(개) → 배송비(원).
   초기: 5만원 이상 무료, 아니면 3000원 + 4개째부터 개당 500원. *)

[정수 주문금액을 정수 수량을 배송비]는 -> 정수 {
    주문금액 >= 50000이면 { 반환 0. }
    정수 요금 = 3000.
    수량 > 3이면 { 요금 = 요금 + 500 * (수량 - 3). }
    반환 요금.
}

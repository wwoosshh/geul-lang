포함 "std.gl"

[시작하기]는 -> 정수 {
    "=== 에러 처리 예제 ===\n"을 쓰다.

    정수 값 = 10.
    값 == 0이면 {
        "값이 0입니다\n"을 쓰다.
    }
    값 != 0이면 {
        "값이 0이 아닙니다: %d\n"을 값을 쓰다.
    }

    "에러 처리 완료\n"을 쓰다.
    반환 0.
}

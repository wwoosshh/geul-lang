포함 "표준/문자열.gl"
[시작하기]는 -> 정수 {
    문자열 위치 = 문자열_찾기("hello world" 에서, "wor").
    "%s\n"을 위치를 쓰다.
    문자열_포함("가나다" 에서, "나")이면 { "포함\n"을 쓰다. }
    아닌 문자열_포함("가나다" 에서, "라")이면 { "미포함\n"을 쓰다. }
    문자열 c = 문자_찾기("a/b/c", 47).
    문자열 r = 문자_역찾기("a/b/c", 47).
    "%s %s\n"을 c를 r를 쓰다.
    반환 0.
}

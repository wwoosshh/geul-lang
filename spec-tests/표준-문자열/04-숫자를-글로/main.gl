포함 "표준/문자열.gl"
[시작하기]는 -> 정수 {
    문자[32] 버퍼.
    정수를_글로(버퍼, 32 로, -42).
    "%s\n"을 버퍼를 쓰다.
    실수를_글로(버퍼, 32, 2.5).
    "%s\n"을 버퍼를 쓰다.
    실수를_정밀글로(버퍼, 32, 3.14159, 2).
    "%s\n"을 버퍼를 쓰다.
    반환 0.
}

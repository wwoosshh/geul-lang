포함 "표준/문자열.gl"
[시작하기]는 -> 정수 {
    문자열 합 = 문자열_합치기("안녕, ", "세계").
    "%s\n"을 합을 쓰다.
    문자열 부분 = 문자열_자르기("abcdefg", 2, 3).
    "%s\n"을 부분을 쓰다.
    문자열 끝 = 문자열_자르기("abc", 2, 100).
    "%s\n"을 끝을 쓰다.
    해제(합). 해제(부분). 해제(끝).
    반환 0.
}

포함 "표준/기본.gl"
[정수 a를 정수 b를 정수 c를 정수 d를 정수 e를 정수 n을 누적]은 -> 정수 {
    n == 0이면 { 반환 a + b + c + d + e. }
    반환 누적(a + 1, b, c, d, e + 1, n - 1).
}
[시작하기]는 -> 정수 {
    "%d\n"을 누적(0, 0, 0, 0, 0, 3)을 쓰다.
    반환 0.
}

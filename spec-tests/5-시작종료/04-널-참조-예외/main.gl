(* 처리되지 않은 예외는 메시지를 내고 종료 코드 3 으로 끝난다 (D-26).
   전에는 코드 배치에 따라 조용히 0 을 돌려주기도 했다 — 생성한 실행 파일에 예외 정보가 없어서다. *)
[시작하기]는 -> 정수 {
    "시작\n"을 쓰다.
    정수 참조 널 = 없음.
    정수 v = 널[0].
    "여기는 오지 않는다 %lld\n"을 v를 쓰다.
    반환 0.
}

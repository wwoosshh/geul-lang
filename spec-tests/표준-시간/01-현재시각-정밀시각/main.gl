포함 "표준/시간.gl"
[시작하기]는 -> 정수 {
    정수 t = 현재시각().
    실수 a = 정밀시각_밀리초().
    잠자기(20).
    실수 b = 정밀시각_밀리초().
    "%d %d %d\n"을 (t > 1700000000)을 (b - a >= 15.0)을 (초당틱 == 1000)을 쓰다.
    "%d\n"을 (경과틱() >= 0)을 쓰다.
    반환 0.
}

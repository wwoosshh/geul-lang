[시작하기]는 -> 정수 {
    문자열 버퍼 = 할당(16).
    메모리_채우기(버퍼, 65 로, 5).
    버퍼[5] = 0.
    "{버퍼}\n"을 쓰다.
    문자열 둘 = 초기할당(4 를, 4).
    "%d\n"을 둘[3]을 쓰다.
    버퍼 = 재할당(버퍼, 64).
    문자열_복사(버퍼 + 5 에, "BCD").
    "{버퍼} {글자수(버퍼)}\n"을 쓰다.
    해제(버퍼).
    해제(둘).
    반환 0.
}

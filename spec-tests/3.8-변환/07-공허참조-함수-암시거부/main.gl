포함 "표준/기본.gl"

(* 공허 참조 → 함수는 오직 '으로' 명시 변환만 연다. 대입(암시 변환)은 막는다.
   이 부정 테스트가 통제 경계를 지킨다 — 실수로 암시 변환이 열리면 여기서 깨진다. *)

별칭 코드조각는 [정수 -> 정수].

[시작하기]는 -> 정수 {
    공허 참조 p = 없음.
    코드조각 f = p.
    반환 0.
}

포함 "표준/기본.gl"
[시작하기]는 -> 정수 {
    정수 i = 0.
    정수 s = 0.
    (i < 5)동안 {
        i를 증가하다.
        갈래 (i) {
            경우 3: { 계속. }
            기본: { s = s + i. }
        }
        s = s + 100.
    }
    "%d\n"을 s를 쓰다.
    반환 0.
}

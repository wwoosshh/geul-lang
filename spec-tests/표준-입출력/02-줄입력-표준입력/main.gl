포함 "표준/입출력.gl"
[시작하기]는 -> 정수 {
    문자[64] 줄.
    정수 n = 줄입력(줄, 64).
    "%lld [%s]\n"을 n을 줄을 쓰다.
    n = 줄입력(줄, 64).
    "%lld [%s]\n"을 n을 줄을 쓰다.
    n = 줄입력(줄, 64).
    "%lld [%s]\n"을 n을 줄을 쓰다.
    반환 0.
}

(* 표준 라이브러리 — 체계. 포함 "표준/체계.gl" *)

외부 "rand"   [난수]는 -> 중간정수.
외부 "srand"  [중간정수 씨앗을 난수씨앗]는.
외부 "getenv" [문자열 이름을 환경변수]는 -> 문자열.

(* 0 이상 상한 미만의 난수 *)
[정수 상한을 난수_범위]는 -> 정수 {
    상한 <= 0이면 { 반환 0. }
    반환 난수() % 상한.
}

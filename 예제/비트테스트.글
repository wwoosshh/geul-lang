(*
 * 비트 연산 테스트
 *)

포함 "표준/입출력.글"

[시작하기]는 {
    정수 가 = 0xFF00.
    정수 나 = 0x0F0F.

    "AND: %X\n"을 (가 & 나)를 쓰기다.
    "OR:  %X\n"을 (가 | 나)를 쓰기다.
    "XOR: %X\n"을 (가 ^ 나)를 쓰기다.
    "NOT: %X\n"을 (~가)를 쓰기다.
    "SHL: %X\n"을 (1 << 8)을 쓰기다.
    "SHR: %X\n"을 (가 >> 4)를 쓰기다.
}

(*
 * 메모리 표준 라이브러리
 * 동적 메모리 관리를 위한 한글 인터페이스
 *)

[정수 바이트를 할당]는 -> 공허 참조 {
    반환 malloc(바이트).
}

[정수 개수를 정수 바이트로 초기할당]는 -> 공허 참조 {
    반환 calloc(개수, 바이트).
}

[공허 참조 메모리를 정수 바이트로 재할당]는 -> 공허 참조 {
    반환 realloc(메모리, 바이트).
}

[공허 참조 메모리를 해제]는 {
    free(메모리).
}

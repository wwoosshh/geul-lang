# 글 2세대 완성 기록

2026-09-21. 글 2세대는 1.2.0에서 완성이다. 쓸모를 향한 개발은 여기서 접는다.
이 문서는 왜 여기가 끝인지, 무엇이 남는지를 적는다 — 만들다 만 것이 아니라 완성한 것임을 분명히 하려고.

## 무엇이 완성됐나

한글 키워드와 한국어 어순(SOV)으로 쓰는 시스템 언어, 그리고 그 언어로 쓴 컴파일러.

- **자체호스팅.** 글로 쓴 글 컴파일러가 자기 자신을 컴파일해 참조 구현(파이썬)과 바이트가 같은 실행 파일을 낸다. 1·2·3세대 고정점. C 컴파일러 없이 x86-64 PE를 직접 만든다.
- **언어.** 묶음·참조·함수 포인터·제네릭·조각·결과 타입, 조사가 인자 역할을 정하는 호출(D-30: 바꿔 쓰면 데이터가 날아가는 자리를 문법이 거부한다), 동사형 호출, 범위 반복, 식 보간. 문법과 표준 라이브러리는 [호환 약속](docs/11-호환-약속.md)으로 얼려 있다.
- **런타임.** C 런타임 없이 kernel32 위에 글로 썼다. 창 프로그램(user32·gdi32)과 Windows 콜백까지 된다 — 첫 실제 도구는 백업 프로그램(CLI·GUI).
- **실행 중 함수 교체(1.2.0).** `--핫스왑`이면 내부 호출이 함수 테이블을 거치고, `교체(함수, 새주소)`가 슬롯을 바꾼다. 재시작·상태손실 없이 코드만 갈린다. 참조 구현엔 `--조각`(순수 잎 함수 → 위치 독립 원시 코드)도 있다. 콘솔 생명 게임과 요금 규칙 튜닝기에서 실제로 돌았다.
- **검증.** 수용 테스트 194개(양쪽 컴파일러), 덤프·호출 색인·위험 보고 교차 비교, 오류 메시지 줄·열 일치, 실행 파일 바이트 비교, 문서 예제 빌드 검증, CI.
- **기록.** 헌장, 문법 명세, 설계 결정 D-01~D-45, 1세대 교훈, 읽기 쉬움 점검, 시장조사, 연구 중단 기록, 핫스왑 방향, 대중성 조사.

## 왜 여기서 접는가

기준은 하나였다: **쓸만하고, 이왕이면 엄청난 것.** 장인정신은 만드는 방식이지 쓸모없음의 면죄부가 아니다.

엄청난가 — 그렇다. 쓸만한가 — 근거가 없고, 더 만들어서 생기지도 않는다.

1. **세 방향이 같은 시험에 떨어졌다.** 검토 언어("왜 라이브러리·AI가 아니라 언어인가"), 핫리로드("왜 변수 수정이 아니라 코드 교체인가, 비싼 상태가 어디 있나"), 게임 엔진 언어("왜 씬 분할이 아니라 시간 여행인가"). 셋 다 더 단순한 진화된 수단이 이미 있었고, 셋 다 **글이라는 자산에서 출발**했다는 같은 원인이었다. [연구 중단 기록](연구-중단-기록-2026-09-09.md)이 이미 진단한 것이다: 자산에서 출발하지 않는다.
2. **원래 사명은 불가능했다.** 글은 "영어 뿌리 나무의 한글 가지가 아니라 뿌리부터 한글인 별개의 나무"로 태어났다. 그러나 뿌리는 CPU·펌웨어·유니코드까지 내려가고, 그 바닥은 바꿀 수 없다. 별개의 나무는 원리상 서지 않는다.
3. **고쳐 세운 사명은 일관하지만 상한이 있다.** "두 언어, 하나의 컴퓨팅" — 한국어가 기반 위에서 영어와 동등한 표면이 되는 것. 글은 이미 그 모델이다(한국어 문법 표면 + x64 기반 + extern 교환). 그런데 그 형태의 대중성 상한은 이미 측정돼 있다: 일본어(같은 SOV·조사 구조)의 なでしこ가 2005년부터 조사로 인자를 묶었고, 20년 뒤 업무 자동화 취미와 교과서 한 권에 머물렀다. 한국은 50년간 15개 넘게 시도해 전부 연구·교육·취미에서 멈췄고, 개발자 대중은 구조적 이유(네트워크 효과)로 거의 만장일치 반대다. 이유는 언어의 품질과 무관하므로 글을 더 잘 만들어도 바뀌지 않는다. [대중성 조사](docs/15-대중성-조사.md).

정당하지 않은 건 하나뿐이었다: 쓸모를 근거 없이 전제하고 더 만드는 것. 그래서 접는다.

## 글은 무엇인가

**연구·예술 프로젝트.** 위안이 아니라 정확한 분류다. 한글 프로그래밍 언어의 현재 흐름이 예술·실험(난해 언어)이고, 글은 그 계열에서 가장 진지한 작품이다 — 자체호스팅하고, 고정점을 지키고, 실제 도구를 냈고, 실행 중 함수 교체까지 한다. 시장조사(2026-09-05) §8이 미리 적어 둔 대체 정의 그대로다.

## 남는 기여 — 남이 가져갈 수 있는 것

- **조사 방향 안전성(D-30).** 격이 있는 문법이라야 가능한 검사: 같은 타입의 인자가 역할이 다를 때 호출부가 조사를 안 붙이면 오류. `복사(원본, 대상)`은 영어 표면에서 바꿔 써도 컴파일되지만 글은 거부한다. Swift 인자 라벨·이름 인자가 비슷한 안전을 주므로 유일하진 않으나, 문법의 격을 컴파일 타임 검사로 쓴 사례로서 측정 가능하다(위험 보고 121곳 → 오류 승격, 비용 계측).
- **읽기 쉬움 점검(docs/10).** 컴파일러 10,704줄 중 한국어 문법으로 읽히는 줄이 8%뿐이었고, 어색함이 반복문·출력·표준 이름에 몰렸다는 계측 — 그리고 범위 반복·식 보간(D-34·35)으로 반복문 95%를 한국어 표기로 옮긴 결과.
- **자체호스팅 한국어 어순 네이티브 컴파일러**, 그 자체. 참조 구현과 자기 컴파일이 바이트 동일한 채로 언어를 키우는 방법(덤프 교차 비교·고정점)을 보였다.
- **방법.** 언어여야 하는 이유를 처음에 묻고, 자산에서 출발하지 않고, 구현보다 사례와 사람을 먼저 두며, 판정 기준을 결과 전에 정한다 — 세 번 실패하며 얻은 것이고, 이 프로젝트가 스스로에게 적용한 것이다.

## 접히지 않는 것

**만드는 사람의 능력.** 혼자서 자체호스팅 네이티브 컴파일러와 런타임을 짓고, 자기 방향을 세 번 정직하게 잘라낸 판단력. 그건 글이 아니라 사람에게 있고, 다음 것으로 그대로 간다. 다음 것은 글에서 출발하지 않는다 — 사람에게 들은 문제에서 출발한다.

## 남기는 상태

- 판: **1.2.0**(1.1.1 + 실행 중 함수 교체). 태그 `2g-1.2.0`, `gen2-final`.
- 호환 약속은 그대로다. `교체` 예약어가 유일한 기록된 예외다(D-43, 11장).
- 빌드·검증: `python build.py test`, `selfhost`, `docs`, `tools`, `release`. 참조 구현만 있는 것: `--조각`.
- 저장소는 남긴다. 더 만들지 않는다. 고칠 것이 있으면 수 번호로 고친다.

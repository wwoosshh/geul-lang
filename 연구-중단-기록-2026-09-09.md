# 연구 방향 전환의 중단 기록

2026-09-09. 이 문서는 2026-09-08부터 이틀간 시도한 연구 방향이 왜 중단됐는지 남긴다.
같은 길로 다시 들어가는 것을 막는 것이 목적이다.

## 저장소의 현재 상태

글은 **1.1.1에서 완성된 상태**로 되돌아왔다. 커밋 `f2e4aae`가 담았던 연구 전환은
`53f8430`으로 되돌렸고, 내용은 `541d377`과 동일하다.

연구 산출물은 가지 `research/review-language`에 보존한다. 이어서 개발하지 않는다.

1.1의 상태는 그대로다. 참조 구현과 자체 컴파일러가 서로를 검증하고, 자기 컴파일 고정점이
성립하며, 수용 테스트 190개가 통과한다. 남은 실패 1건은 `프로그램-프로젝트/06-빌드시스템`이
저장소 밖 컴파일러 경로를 하드코딩해 이 PC의 파일 유무에 걸리는 것이다. 연구 작업과 무관하며
2026-09-08 실증 평가에서도 같은 결과였다.

## 무엇을 시도했는가

출발한 문제 인식은 이것이다. AI가 구현을 대량으로 만들어 내면서 사람이 검토를 감당하지
못한다. 이 결핍을 글이라는 언어로 해결하려 했다.

문제 인식 자체는 지금도 유효하다고 본다. 2025년 DORA 보고서는 AI 도입이 처리량과 양의
관계를, 안정성과 음의 관계를 갖는다고 보고한다. METR의 무작위 실험은 숙련 개발자가 AI를 쓸 때
느려졌고 본인은 빨라졌다고 믿었다는 결과를 냈다. 다만 두 자료 모두 검토가 산업 전체의 지배적
병목이라고 측정한 것은 아니며, 글에 대한 수요 근거도 아니다.

중단한 이유는 문제가 아니라 수단이다. **이 결핍은 언어 모양의 문제가 아니다.**

## 여덟 갈래와 각각의 탈락 이유

| 갈래 | 탈락 이유 |
|---|---|
| 범용 한국어 언어 | 배우고 계속 쓸 이유를 제시하지 못함 |
| 승인 유효성을 실행 의미에 넣는 언어 | LBAC, PACE, POLARIS, TACIT 등 2026년 선행 연구가 선점 |
| AI의 미합의 선택 노출 | AssumptionMiner, ClarifyCodeBench 등이 이미 다룸. 다만 판단 기록 부분은 미탐색 |
| C·Java를 글로 올리기 | 문제 영역이 시장과 어긋나고 1인 개발 규모를 크게 넘음 |
| TypeScript 해석기 | 규칙으로 임의 코드를 요약하면 LLM에 넓이로 짐. 좁히면 실제 코드를 못 다룸 |
| 표기법 전환 | 글일 필요가 없음. 기계어와 달리 TypeScript는 이미 사람이 읽는 표기법이라 여유가 작음 |
| 제약을 거는 저작 언어 | React와 XState가 이미 차지. 언어의 전면적 제약은 웹앱의 외부 통로 때문에 성립 못 함 |
| 변경 거르개 | 모듈 경계가 영향 범위를 이미 알려줌. 새로 쓰인 코드에는 비교할 이전이 없어 무력 |

여덟 번 모두 같은 시험에서 떨어졌다. **왜 라이브러리나 프레임워크나 AI가 아니라 언어여야
하는가.** 이 시험을 마지막에 적용했기 때문에 매번 구현을 먼저 만들고 나서 탈락했다.

## 실제로 만든 것

두 겹이 있다.

**G0.** C의 정수 결정 규칙을 다루는 작은 언어다. 문법, 파서, 독립 실행기, C 생성기, 여러
파일의 상수 함수 연결, 모델 전 범위 동등성 검사가 있다. 작지만 언어의 형태를 갖췄다.

**web-review.** TypeScript와 React를 대상으로 한 분석기다. 명령 23개, 올리기 모드 18개,
단위 검사 275개, 소스 약 4,500줄이다. Excalidraw와 Actual과 Jitsi의 실제 변경 12건을
고정하고, 그중 2건에서 여러 파일의 조건을 결합한 행동 단위 문서를 만들었다. 조건은 손으로
쓰지 않고 원본 계산에서 도출하며, 원본을 고치면 설명이 따라 바뀐다. 이 성질은 제3자 검증으로
확인했다.

여기서 언어의 성질은 사라졌다. 글 문법을 읽는 파서가 없고 코드 생성기도 없다. 한국어 출력은
내부 표현을 한 방향으로 렌더링한 것이며 되읽을 수 없다. G0가 오히려 원래 목표에 가까웠다.

## 확인한 것과 확인하지 못한 것

확인한 것은 충실도다. 결합한 조건은 원본 실행과 대조했고, 알 수 없는 구문은 추측하지 않고
거부하며, 모든 문장에 원본 위치가 붙는다. 이 성질은 AI 요약에 없다.

확인하지 못한 것이 핵심이다.

- 사람이 실제로 더 쉽게 검토하는지. 참가자 0명, 예행 0회.
- 실제 프로젝트의 변경 중 이 방식이 다룰 수 있는 비율.
- 잠재 사용자의 수요. 인터뷰 0건. 모든 근거가 2차 자료였다.

전체 행동 검증은 12건 중 0건으로 유지했다.

## 방법에서 배운 것

**언어여야 하는 이유를 처음에 묻는다.** 후보를 목록에 올리기 전에 기존 수단으로 왜 안 되는지
한 문장으로 쓸 수 있어야 한다. 쓰지 못하면 조사 대상에서 뺀다.

**자산에서 출발하지 않는다.** 글이 있으니 연구가 글을 써야 한다는 전제가 여덟 갈래를 모두
왜곡했다. 답의 모양을 미리 정하면 문제를 그 모양에 맞추게 된다.

**구현보다 사례와 사람이 먼저다.** 이틀 동안 구현은 빠르게 자랐고 인터뷰는 한 건도 없었다.
검증할 수 있는 쪽으로만 자란 결과가 지금의 분석기다.

## 다음

연구는 글을 기반으로 하지 않고 별도 프로젝트로 진행한다. 한국어여야 한다는 것, 언어여야
한다는 것, 기존 컴파일러를 써야 한다는 것을 물려받지 않는다. 물려받는 것은 위의 방법뿐이다.

첫 단계는 만들 것을 정하지 않은 조사다. 누가 무엇 때문에 곤란한지를 사람에게 직접 듣는다.

글 1.1은 완성된 결과물로 남긴다. 시장이 없다는 것과 만들다 만 것은 다르다.
